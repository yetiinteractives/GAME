using UnityEngine;

public class CSSceneChanger : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        SceneLoader.Load("VikingVillage");
        
    }
    

    // Update is called once per frame
    void Update()
    {
        
    }
}
